@model IEnumerable<DienMayQT.Models.CashBillDetail>

    <table class="table">
        <tr>
            <th>
                Số lượng
            </th>
            <th>
               Giá bán
            </th>

            <th>
               Mã sản phẩm
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @String.Format("{0:0,0} VNĐ", item.SalePrice)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Product.ProductCode)
            </td>
            <td>
                @Html.ActionLink("Xóa", "Delete", new { id = item.ID })
            </td>
        </tr>
    }
    
    </table>
<div>
    <div class="form-group" align="right">
        <label class="control-label col-md-2">Tổng tiền</label>
        <div class="col-md-10">
            <input class="disabled" value="@{var total = Model.Sum(m => m.SalePrice * m.Quantity);
                                                Session["total"] = total;
                                                @String.Format("{0:0,0 VNĐ}",total)}" disabled /> 
        </div>
    </div>
</div>
<script>
    var total = 
</script>